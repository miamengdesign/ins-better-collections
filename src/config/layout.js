/** Extra scroll distance past 100vh that drives the Hero sticky-card exit. */
export const HERO_EXIT_VH = 60

/** Full Hero track height — Section 02 overlaps by this amount to sit underneath. */
export const HERO_TRACK_VH = 100 + HERO_EXIT_VH
